# دليل تشغيل سريع — تجربة نقاط البيع في المطعم

هذا دليل مختصر لتشغيل النظام على جهاز كمبيوتر واحد في المطعم (كاشير) خلال دقائق، لتجربته
عملياً واكتشاف أي ملاحظات قبل الاعتماد عليه بشكل دائم.

## 1) التثبيت (مرة واحدة فقط)

1. ثبّت **Docker Desktop** على جهاز الكاشير (ويندوز أو ماك): https://www.docker.com/products/docker-desktop/
   بعد التثبيت شغّله وتأكد أنه يعمل (أيقونة الحوت في شريط المهام).
2. حمّل مجلد المشروع كاملاً إلى الجهاز (فك الضغط إن كان بصيغة zip).

## 2) التشغيل

افتح **Terminal** (ماك) أو **PowerShell** (ويندوز) داخل مجلد المشروع، ثم:

```bash
docker compose up -d
```

أول مرة سيأخذ بضع دقائق (يُنزّل الصور ويجهّز قاعدة البيانات تلقائياً). بعدها افتح المتصفح على:

```
http://localhost:4000
```

بيانات الدخول الافتراضية:
- البريد: `admin@example.com`
- كلمة المرور: `ChangeMe123!`

> **مهم:** غيّر كلمة المرور هذه لاحقاً (سيُضاف لاحقاً شاشة لتغييرها من داخل النظام — حالياً
> يمكن تغييرها بإعادة البذر بمتغيرات مختلفة، راجع القسم 5 أدناه).

## 3) الإعداد الأول قبل البيع

1. **الإعدادات** → أدخل اسم المطعم والرقم الضريبي (مطلوب لإصدار الفواتير) واحفظ.
2. **المخزون → المستودعات** → أضف مستودعاً واحداً على الأقل (مثال: "المطبخ الرئيسي").
3. **المخزون → الأصناف والأرصدة** → أضف أصناف قائمة الطعام (الاسم، السعر شامل الضريبة،
   "يُباع في نقاط البيع؟ = نعم"). إن رغبت بتتبّع المخزون الفعلي لأي صنف، استخدم قسم
   "استلام / تسوية مخزون يدوية" أسفل نفس الصفحة لتحميل كمية ابتدائية.
4. **نقاط البيع → نقطة البيع** → اختر الكاشير (أو أنشئ واحداً) والمستودع، وابدأ البيع.

## 4) الوصول من تابلت/جوال آخر على نفس الشبكة (اختياري)

إن أردت البيع من تابلت بدلاً من جهاز الكاشير مباشرة:
1. تأكد أن التابلت متصل بنفس شبكة الواي فاي.
2. اعرف عنوان IP الخاص بجهاز الكاشير على الشبكة المحلية (مثال: `192.168.1.10`).
3. من متصفح التابلت افتح: `http://192.168.1.10:4000`

## 5) تخصيص بيانات الدخول الأولى (اختياري)

قبل أول تشغيل، أنشئ ملف `.env` بجانب `docker-compose.yml` بهذا المحتوى (عدّل القيم):

```
JWT_SECRET=اكتب-هنا-نص-عشوائي-طويل
SEED_ADMIN_EMAIL=owner@yourrestaurant.com
SEED_ADMIN_PASSWORD=كلمة-مرور-قوية
```

## 6) إيقاف/إعادة تشغيل النظام

```bash
docker compose stop      # إيقاف مؤقت (البيانات محفوظة)
docker compose up -d     # إعادة التشغيل
docker compose down      # إيقاف وحذف الحاويات (البيانات تبقى محفوظة في volume منفصل)
```

## ما هو غير مكتمل بعد (توقّعه أثناء التجربة)

- **الطباعة**: الفاتورة تُطبع عبر طباعة المتصفح العادية حالياً، وليست طباعة حرارية مباشرة
  لطابعة إيصالات (ESC/POS). إن كان لديكم طابعة إيصالات حرارية أخبرني بالموديل لإضافة الدعم.
- **مستخدم واحد فقط بصلاحيات كاملة**: طبقات الصلاحيات (كاشير محدود الصلاحيات مثلاً) لم
  تُبنَ بعد.
- **لا يوجد وضع عمل بدون إنترنت/بدون اتصال بقاعدة البيانات** — إن توقف جهاز الكاشير أو
  الشبكة الداخلية، تتوقف نقاط البيع حتى تعود قاعدة البيانات.
- كل ملاحظة أو خطأ تكتشفونه أثناء الاستخدام الفعلي في المطعم، دوّنوه (الخطوات لإعادة
  إنتاجه إن أمكن) لأرسله لي لإصلاحه.
